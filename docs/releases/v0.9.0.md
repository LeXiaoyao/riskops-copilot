# v0.9.0 — M9 Phase 1 Must 收口

发布日期：2026-05-28

## 本版本交付

### M9-A 异常埋点补齐到 7 条
- `riskops/data/synth/`：合成数据最近 30 天显式埋入「高余额高风险客群占比 +8pct」结构性偏移
- `riskops/engines/anomaly/detector.py`：新增 `high_balance_high_risk_share` 因子检测，severity ≥ medium
- 异常总数从 6 提升到 7；短信模板投诉信号继续由 QC 11 维评分覆盖，不重复建设

### M9-B PPT 周报扩到 9 页 + 讲稿备注
- `riskops/engines/report/ppt_renderer.py`：m4_business_report.pptx 9 页
- 每页含「标题 + 结论 + 图表建议 + 讲稿备注」，notes_slide 全覆盖、无 TODO / 占位符
- 讲稿内容拼接 M3 summary / 归因结果 / ROI 沙盘

### M9-C Excel 周报新增明细表与透视表
- `riskops/engines/report/excel_renderer.py`：weekly_report.xlsx 由 4 sheet 扩到 6 sheet
- 新增「明细_案件」（DWS 案件宽表抽样 201 行）
- 新增「维度透视_供应商×线路」（行 vendor × 列 region，回收率值，末行末列 SUM 公式）

### M9-D TUI 命令集补齐到 13 条
- `riskops/tui/app.py`：新增 `/qc`、`/script`、`/vendor`、`/report` 四条 slash 命令
- `/report` 通过 `asyncio.to_thread` 异步触发周报全量导出，不阻塞主循环
- 缺失输出文件时返回友好提示而非栈

### M9-E 话术质检雷达图
- `riskops/engines/visualization/chart_builder.py`：新增 `build_qc_quality_radar_chart`
- 输出 `outputs/visualization/qc_quality_radar.html`，Plotly scatterpolar 六维图
- 六维：合规性 / 强度 / 清晰度 / 同理 / 完整 / 流程
- 必做 10 张图最后一张补齐；双主题切换不在本期范围

### M9-F 飞书友好 Markdown 导出
- `riskops/engines/report/feishu_md_renderer.py`：独立 renderer
- 输出 `outputs/reports/weekly_report.feishu.md`
- 去除 GFM 删除线、相对化图片路径、保留表格 + emoji + 行内代码
- 末尾追加飞书协作声明；不调真实飞书 API（遵循 ADR-010）

### M9-G 5 分钟 Demo 脚本
- `docs/demo_script.md`：5 段分镜（开场 / 数据底座 / 异常+归因 / 策略 ROI + 话术 / 报告导出）
- 按真实 TUI 13 条命令和 outputs 路径编排
- README 增加 demo 入口链接

## 边界说明
- 本版本仍是 portfolio demo，所有结论基于合成数据，不代表真实业务
- ml-baseline 限定为 readiness baseline，不作为生产模型，不参与策略决策
- LLM 输出仅供参考，所有催收动作需人工审批
- P4 字段（姓名 / 手机 / 身份证 / 地址）不进入 LLM 上下文，不出现在 DWD 以上层

## 测试
- 全测试集：229 个测试（v0.8.0 的 216 + 新增 13），全部通过
- 新增测试覆盖：异常埋点 / PPT 9 页 + 讲稿 / Excel 6 sheet + pivot 公式 / TUI 13 命令 + mock 兜底 / 雷达图六维 / 飞书 MD 内容校验

<!-- C5 DONE -->
